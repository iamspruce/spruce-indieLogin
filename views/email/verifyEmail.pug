extends emailLayout

block content
  p Hi #{me},
  p You are recieving this email because you wanted to log into #{client_id} via: spruceAuth.
  p Open this url: #{url} in a browser or click the link below to log in.
  table.btn.btn-primary(role='presentation' border='0' cellpadding='0' cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation' border='0' cellpadding='0' cellspacing='0')
            tbody
              tr
                td
                  a(href=`${url}`, target='_blank') Continue to Login
  p If you didn't try to log in to: #{client_id}, please ignore this email!